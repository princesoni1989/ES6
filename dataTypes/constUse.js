/**
 * Created by princesoni on 1/29/16.
 */
'use strict'
const x = 100;
//x = 200;// error constant cannot be re-assigned

const obj = {}
obj.name = 'prince soni'
//obj = {} // error constant cannot be re-assigned

const arr = [];
arr.push(100);
//arr = []; // error constant cannot be re-assigned
